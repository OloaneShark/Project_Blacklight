# Legacy CloudGuard Flask Application

This directory preserves the original CloudGuard web application that Project Blacklight evolved from.

It is retained for project history and reference only. The supported Project Blacklight application is the installable CLI under `blacklight_security/` and is run with the `blacklight` command.

The legacy files are not part of the current package architecture and should not be used as the entry point for new Blacklight development.
